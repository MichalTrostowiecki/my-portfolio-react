import Navbar from "../navbar";
import { Card } from "flowbite-react"

const Header = ( { className } ) => {
    return (
        <div>
            <Navbar />
        </div>
    )
}

export default Header;